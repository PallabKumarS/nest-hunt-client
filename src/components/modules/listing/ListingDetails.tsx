import { TListing } from "@/types";

const ListingDetails = ({ listing }: { listing: TListing }) => {
  return (
    <div>
      <h1>This is ListingDetails Component</h1>
    </div>
  );
};

export default ListingDetails;
